#include <stdio.h>
int main()
{
    long long int n, j, k, b;
    long long int a[1000005], c[100005];
    scanf("%d", &n);
    for (int i = 1; i <= n; i++)
    {
        scanf("%d", &k);
        for (int i = 0 ;i < k; i++)
        {
            scanf("%d", &a[i]);
        }
        
        
        
    }
}
