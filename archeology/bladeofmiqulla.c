#include <stdio.h>
int a[200005];
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
    int n,b;
    scanf("%d %d",&n,&b);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }

    }
}